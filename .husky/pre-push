set -e

# Sjekk om CI-miljøvariabelen er satt
if [ "$CI" = "true" ]; then
  echo "CI-miljø oppdaget, hopper over pre-push tester"
  exit 0
fi

pnpm run -s compileTS
pnpm run -s test:prepush
pnpm run csp:check