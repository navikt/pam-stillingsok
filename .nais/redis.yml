apiVersion: aiven.io/v1alpha1
kind: Redis
metadata:
  labels:
    app: pam-stillingsok
    team: teampam
  name: redis-teampam-{{ redis.instance }}
  namespace: teampam
spec:
  plan: {{ redis.plan }}
  project: {{ redis.project }}