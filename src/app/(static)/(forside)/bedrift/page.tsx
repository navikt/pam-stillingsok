import Bedrift from "@/app/(static)/(forside)/bedrift/Bedrift";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Bedrift",
};

export default function Page() {
    return <Bedrift />;
}
